export const MedicationPlanCreateDialogCss = (theme) => ({
    textFieldGridItem: {
        width: '300px',
    },
    textField: {
        marginLeft: theme.spacing(1),
        marginRight: theme.spacing(1),
        width: '90%'
    },
    button: {
        padding: theme.spacing(1),
        margin: theme.spacing(1.5)
    },
    formControl: {
        width: '100%'
    },
    label: {
        marginLeft: theme.spacing(1)
    }
});