export const CaregiverOverviewCss = (theme) => ({
    root: {
        width: '100%',
        marginTop: theme.spacing(3),
    },
    paper: {
        width: '100%',
    },
    table: {
        padding: theme.spacing(1),
    },
    tableWrapper: {
        overflowX: 'auto' as 'auto',
    },
    gridItem: {
        width: '90%',
        padding: theme.spacing(1),
        margin: theme.spacing(5)
    },
    medGridItem: {
        padding: theme.spacing(1),
        margin: theme.spacing(5)
    }
});